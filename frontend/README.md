# WDSL WebToolkit Frontend

React + Vite + Tailwind frontend for the WDSL WebToolkit backend.

See the [project root README](../README.md) for full setup instructions (backend, database, Lighthouse runner, and this frontend together).

```bash
npm install
npm run dev
```

Requires the backend running on `http://localhost:5000`.
